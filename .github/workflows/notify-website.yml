name: Notify Website of Content Update

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh website content cache
        run: |
          curl -s -X POST https://resources.noblecollective.org/api/refresh || true
